-- Trigger the buffer picker using the tab key, creating an intuitive
-- and ergonomic shortcut that matches the visual concept of tabs.
vim.keymap.set("n", "<Leader><Tab>", "<Cmd>BufferLinePick<CR>")
